# AI_CONTEXT.md — Excel Combiner 项目记忆文件

> 📅 生成日期: 2026-06-21  
> 🦞 维护者: 皮皮 (AI Assistant) + 橙月君 (Product Owner)  
> 🎯 项目代号: Excel小表并大表 / Excel Combiner  
> 📦 GitHub: [OrangeMoon-hub/Excel_combiner](https://github.com/OrangeMoon-hub/Excel_combiner)

---

## 一、项目整体目标

### 1.1 一句话定位

**将多个结构相似的 Excel/CSV 小表数据，按列名智能匹配合并到统一的模板大表中，并生成完整的异常审计记录。**

### 1.2 业务背景

橙月君（数据岗位）在日常工作中需要处理来自多个仓库/部门提交的 Excel 报表。这些报表：
- 列名可能不完全一致（多余列、缺失列、同名异位列）
- Sheet 结构可能略有差异
- 合并过程需要可追溯、可审计

手动逐个打开复制粘贴 → 费时、易错、不可审计。因此需要一个零依赖、图形化、双击即用的工具。

### 1.3 核心设计原则

| 原则 | 说明 |
|------|------|
| **零第三方依赖** | 仅使用 Python 标准库（zipfile + xml.etree + tkinter + csv） |
| **单文件可执行** | PyInstaller 打包为单文件 exe，无需安装 Python 环境 |
| **模板驱动** | 大表仅保留表头行作为列名模板，不含数据行 |
| **用户决策闭环** | 所有异常情况（冲突、缺失、超列）均弹窗让用户决策，不静默丢弃 |
| **完整审计追溯** | 异常记录表 + 取消合并记录表，逐条可查 |

### 1.4 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    合并脚本.py (单文件 1340 行)            │
├─────────────────────────────────────────────────────────┤
│  📂 文件扫描层                                            │
│  ├── _scan_work_dir()          扫描工作目录 .xlsx/.csv    │
│  ├── read_xlsx()               零依赖 xlsx 解析           │
│  ├── read_csv()                CSV UTF-8 读取            │
│  └── read_table()              统一入口（自动判断格式）     │
├─────────────────────────────────────────────────────────┤
│  🖥️ GUI 交互层 (tkinter)                                  │
│  ├── _template_file_dialog()   单选模板文件               │
│  ├── _merge_files_dialog()     多选待合并文件             │
│  ├── _extra_cols_dialog()      超表头列数弹窗             │
│  ├── _no_header_dialog()       无列名检测弹窗             │
│  ├── _discarded_cols_dialog()  丢弃列确认弹窗             │
│  ├── _dup_col_multi_dialog()   同名列冲突弹窗             │
│  └── _confirm_overwrite_dialog() 覆盖确认弹窗            │
├─────────────────────────────────────────────────────────┤
│  🧠 合并逻辑层                                            │
│  ├── process_small_table()     单表处理主流程             │
│  ├── _resolve_dup_with_big()   同名列三步判断             │
│  └── _auto_fill_value()        空值自动填充规则           │
├─────────────────────────────────────────────────────────┤
│  📝 输出层                                                │
│  ├── write_xlsx()              零依赖 xlsx 写入           │
│  ├── 合并结果.xlsx             按 Sheet 对齐的合并数据      │
│  ├── 异常记录 Sheet            四种异常类型完整记录        │
│  ├── 取消合并的表名 Sheet       被跳过/取消的文件列表       │
│  └── 合并日志.txt              控制台日志落盘              │
└─────────────────────────────────────────────────────────┘
```

---

## 二、已完成的功能模块

### 2.1 项目文件结构

```
Excel_combiner/                          ← 项目根目录
├── 合并脚本.py                           ← 主程序 (1340 行，零依赖)
├── PRD_Excel小表并大表工具.md             ← 产品需求文档 (v1.1)
├── README.md                            ← 项目主页文档
├── AI_CONTEXT.md                        ← 本文件（项目记忆）
├── 测试文档/
│   └── 测试方案.md                       ← 19 项测试用例 + 覆盖率矩阵
├── 测试环境/
│   ├── 被填充表.xlsx                     ← 大表模板（NAME1/NAME2）
│   ├── 小表.xlsx                        ← 正常数据小表
│   ├── 小表2.xlsx                       ← 异常数据小表（含超列）
│   └── 小表3.xlsx                       ← 边界数据小表（同名列/特殊值）
├── .gitignore
├── LICENSE (MIT)
└── README.md
```

### 2.2 核心代码模块清单

| 模块/函数 | 行数范围 | 职责 | 状态 |
|-----------|---------|------|------|
| `read_xlsx()` | L81-149 | 零依赖 xlsx 解析（zipfile + ElementTree） | ✅ |
| `read_csv()` | L63-72 | CSV UTF-8 读取 | ✅ |
| `read_table()` | L74-79 | 统一读取入口（自动判断 .xlsx / .csv） | ✅ |
| `write_xlsx()` | L161-300 | 零依赖 xlsx 写入（手动构建 XML） | ✅ |
| `_col_letter()` | L151-159 | 列索引 → Excel 列字母转换（0→A, 25→Z, 26→AA） | ✅ |
| `_scan_work_dir()` | L309-318 | 扫描工作目录文件，排除 ~$ 临时文件 | ✅ |
| `_template_file_dialog()` | L321-379 | 模板文件单选对话框（tkinter Listbox） | ✅ |
| `_merge_files_dialog()` | L381-495 | 待合并文件多选对话框（Checkbutton + 全选 + 勾选变色） | ✅ |
| `_read_big_table()` | L497-519 | 读取并校验大表模板（首列必须为"表名"） | ✅ |
| `process_small_table()` | L948-1131 | 单表合并主流程（Sheet匹配→列映射→异常检测→逐行填充→缓存写入） | ✅ |
| `_resolve_dup_with_big()` | L527-609 | **同名列三步判断**（值一致→数量对等→逐列弹窗） | ✅ |
| `_dup_col_multi_dialog()` | L611-712 | 同名列多列选择弹窗 | ✅ |
| `_extra_cols_dialog()` | L723-783 | 超出表头列数弹窗（按额外列逐列记录） | ✅ |
| `_no_header_dialog()` | L803-855 | 无列名检测弹窗（表头空但数据有值） | ✅ |
| `_discarded_cols_dialog()` | L857-915 | 丢弃列确认弹窗（小表有但大表无的列） | ✅ |
| `_auto_fill_value()` | L926-946 | 空值处理（None/空串→空字符串） | ✅ |
| `_sample_vals()` | L520-526 | 异常记录取前 5 行值 + 总行数备注 | ✅ |
| `main()` | L1133-1323 | 主流程编排（初始化→扫描→模板校验→多选→逐表合并→生成结果） | ✅ |

### 2.3 已实现的核心功能

#### ✅ 文件管理
- 自动扫描工作目录中所有 `.xlsx` / `.csv` 文件
- 排除 `~$` 开头临时锁文件
- 图形化单选模板 + 多选待合并文件（全选/勾选变色）

#### ✅ 数据解析
- 零依赖 xlsx 解析（zipfile + xml.etree.ElementTree）
- CSV UTF-8 读取，映射为虚拟 Sheet `Sheet1`
- 命名空间兼容处理（支持 `ns0` 前缀等异常命名空间）

#### ✅ Sheet 名称匹配
- 大表 Sheet 名为基准，小表必须完全一致才合并
- 小表缺少 Sheet → 跳过该 Sheet
- 小表多余 Sheet → 忽略
- 全部不匹配 → 取消合并该小表，记录到"取消合并的表名"

#### ✅ 列名匹配合并
- 按列名（表头文字）精确匹配，含空格区分（"T" ≠ "T " ≠ "T T"）
- "表名"列自动填入来源文件名（仅去最后一个 `.xlsx` 后缀）
- 文件名含多个 `.` 时正确处理（如 `广州.天河.xlsx` → `广州.天河`）

#### ✅ 同名列处理 — 三步判断逻辑
| 步骤 | 场景 | 行为 |
|------|------|------|
| 第一步 | 同名多列值完全一致 | 自动填入，不弹窗 |
| 第二步 | 小表同名列数 = 大表同名列数 | 按顺序一一对应填入 |
| 第三步 | 小表同名列数 > 大表同名列数 | 逐列弹窗让用户选择：填入列1/列2/忽略/取消合并 |

#### ✅ 异常检测与处理

| 异常类型 | 检测时机 | 用户操作选项 | 记录方式 |
|----------|---------|-------------|---------|
| 超表头列数 | 数据行列数 > 表头列数 | 忽略多余列 / 取消合并该表 | 按额外列逐列记录 |
| 无列名 | 表头有空列名但该列有数据 | 忽略该列 / 取消合并该表 | 标注"数据异常，无列名" |
| 列名不存在于大表 | 小表列名在大表中找不到 | 忽略并继续 / 取消合并该表 | 记录列名 + 前5行值 |
| 同名列未选择 | 同名列弹窗落选的列 | 计入异常记录 | 标注"同名列未选择" |

#### ✅ 异常记录格式
所有异常统一记录到"异常记录"Sheet，含 10 个字段：
`文件名 | Sheet | 行号 | 列名 | 列1位置 | 列1值 | 列2位置 | 列2值 | 异常类型 | 用户操作`

其中 `列1值` 取该列前 5 行实际数据 + 尾注 `(共N行)`。

#### ✅ 输出文件
- `合并结果.xlsx`：模板表头 + 所有合并数据行，按 Sheet 分页
- `异常记录` Sheet：四种异常类型完整记录
- `取消合并的表名` Sheet：被取消/跳过的文件及原因
- `合并日志.txt`：完整运行日志

#### ✅ 安全措施
- 大表快照保护：启动时完整读入内存，后续合并不受外部修改影响
- 源文件全程只读不写
- 输出文件与源文件分离
- 覆盖前弹窗确认

### 2.4 验证记录

| 阶段 | 内容 | 结果 |
|------|------|------|
| 单元测试 | 同名列三步判断 4 项 | ✅ |
| 测试方案 | 19 项结构化测试用例，6 大场景维度 | ✅ |
| E2E 第 1 轮 | Windows 实机运行，修复 Excel 修复弹窗问题 | ✅ |
| E2E 第 2 轮 | XML 命名空间覆盖（ns0 前缀） | ✅ |
| E2E 第 3 轮 | 同名列三步判断 + 异常记录信息密度提升 | ✅ |
| E2E 第 4 轮 | 图形化文件选择 + CSV 支持 + tkinter 跨平台 | ✅ |
| E2E 第 5 轮 | Windows + Linux 双平台，实际业务数据 | ✅ |
| 手动验收 | 橙月君以真实数据岗位场景实测 | ✅ |

---

## 三、当前状态 & 卡点

### 3.1 当前版本

- **版本号**: v1.1（PRD 版本）
- **Git Tag**: 无（最新 commit: `d0d9ae7`）
- **分支**: `main`（唯一分支）

### 3.2 当前 task

**🔄 进行中：反向填表功能（重点）**

目标：将合并结果表的数据反向写入到原始的多 Sheet Excel 文件中，实现"数据刷新"。

### 3.3 已知卡点 / 技术债

| 卡点 | 描述 | 影响 | 优先级 |
|------|------|------|--------|
| 单文件膨胀 | 所有代码在 1340 行的单文件中，模块边界模糊 | 维护困难，新功能难以插入 | P2 |
| 无单元测试框架 | 测试全靠手动 + E2E，无自动化回归 | 重构风险高 | P2 |
| tkinter 跨平台坑 | `padx`/`pady` 元组在 Windows 上报 `bad screen distance` | 已记录并在代码中规避，但需持续注意 | P1 |
| Excel 命名空间兼容 | 某些 Excel 保存的文件带 `ns0` 前缀，解析需额外处理 | 已修复，但不确定是否覆盖所有 Excel 版本 | P2 |
| 无配置文件 | 所有行为硬编码，无用户自定义选项 | 灵活性不足 | P3 |

### 3.4 关联项目

`/home/ubuntu/excel-editor/` 是一个脚手架项目（所有文件为空），似乎是计划中的模块化重构版本，目前未启动。

---

## 四、重要技术决策 & 踩过的坑

### 4.1 技术决策记录

| 决策 | 内容 | 理由 | 日期 |
|------|------|------|------|
| TD-001 | 零第三方依赖，仅标准库 | 目标用户无 Python 环境，需打包 exe 直接运行 | 2026-06-07 |
| TD-002 | xlsx 解析用 zipfile + ElementTree | openpyxl 虽方便但是第三方库，增加打包体积和依赖 | 2026-06-07 |
| TD-003 | 单文件架构（1340 行一个 .py） | 简化打包和分发，用户只需一个文件 | 2026-06-07 |
| TD-004 | 内存缓存后一次性写入 | xlsx 是 zip 格式无法逐行追加；缓存后整文件重建更简单高效 | 2026-06-07 |
| TD-005 | 同名列三步判断而非直接弹窗 | 减少用户操作次数：值一致/数量对等自动处理，仅真正冲突时才弹窗 | 2026-06-14 |
| TD-006 | 大表快照保护（启动时全量读入内存） | 防止合并过程中大表被外部修改导致数据不一致 | 2026-06-07 |
| TD-007 | tkinter 而非其他 GUI 框架 | Python 标准库自带，零额外依赖 | 2026-06-14 |
| TD-008 | CSV 映射为虚拟 Sheet `Sheet1` | 统一处理逻辑，CSV 天然无多 Sheet 概念 | 2026-06-14 |

### 4.2 踩坑记录 🕳️

#### 🕳️ 坑 1: tkinter `padx`/`pady` 元组跨平台不兼容
- **现象**: Windows 上运行报 `TclError: bad screen distance`
- **原因**: Windows tkinter 不支持 `pady=(15, 5)` 元组写法，仅支持单整数值
- **解决**: 所有 `padx`/`pady` 使用单个整数值，间距分离写到 `.pack()` 中
- **教训**: tkinter 跨平台差异大，每个平台都要实测
- **已踩次数**: 2 次（PRD 中特别标注为 P0 约束）

#### 🕳️ 坑 2: xlsx XML 命名空间前缀 `ns0`
- **现象**: 某些 Excel 版本保存的 xlsx 文件中 XML 元素带 `ns0:` 前缀，解析时找不到元素
- **原因**: 使用 `{http://...}name` 全限定名查找时，部分文件用 `ns0:name` 加 `xmlns:ns0=...` 声明
- **解决**: 同时尝试全限定名和无命名空间的本地名查找
- **影响范围**: E2E 第 2 轮修复

#### 🕳️ 坑 3: 异常记录信息密度不足
- **现象**: 早期版本异常记录只写列名，用户无法判断数据内容
- **解决**: 升级为记录前 5 行实际数据值，末尾标注总行数 `(共N行)`
- **影响范围**: E2E 第 3 轮修复

#### 🕳️ 坑 4: 文件名含多个 `.` 的后缀处理
- **现象**: `广州.天河.xlsx` 去后缀后变成 `广州` 而非 `广州.天河`
- **原因**: 用 `split('.')[0]` 简单处理
- **解决**: 仅去除最后一个 `.xlsx` / `.csv` 后缀 → `_basename_no_ext()` 函数
- **影响范围**: 早期版本，已修复

#### 🕳️ 坑 5: xlsx 共享字符串表 (Shared Strings) 命名空间
- **现象**: `xl/sharedStrings.xml` 中部分元素无命名空间前缀，部分有，取值逻辑复杂
- **解决**: 同时检查 `t` 属性、`{http://...}t` 属性、直接文本内容、`is` 元素
- **影响范围**: `read_xlsx()` 核心解析逻辑

---

## 五、下一步：反向填表功能

### 5.1 功能概述

**目标**: 将合并结果表的数据反向写回到原始的多 Sheet Excel 文件中，实现"数据刷新"效果。

### 5.2 业务场景

当前工作流是"多张小表 → 合并为一张大结果表"。但实际业务中，还存在反向需求：
- 某张 Excel 中有多个 Sheet（如按仓库/部门分 Sheet）
- 每个 Sheet 的结构与合并结果表某一 Sheet 的结构相同（一对一对应）
- 需要将合并结果表中的最新数据"刷新"到这张 Excel 的各个 Sheet 中
- 不是简单的覆盖，而是**智能裁剪替换**：根据时间列对齐数据范围

### 5.3 核心功能设计

#### 5.3.1 基本流程

```
合并结果.xlsx ─────────────────┐
                               ▼
                 ┌─────────────────────────┐
                 │    反向填表引擎           │
                 │                          │
                 │  1. 匹配 Sheet（一对一）   │
                 │  2. 智能裁剪（按时间列）   │
                 │  3. 删除旧数据行           │
                 │  4. 填入新数据行           │
                 │  5. 生成新文件             │
                 └────────────┬────────────┘
                              ▼
                 目标文件_已刷新.xlsx
```

#### 5.3.2 详细需求

##### A. Sheet 匹配（一对一）

- 用户选择一个"待填入的目标 Excel 文件"（含多个不同 name 的 Sheet）
- 用户选择一个"合并结果 Excel 文件"（含多个 Sheet）
- 系统自动或手动建立 Sheet 之间的**一对一映射关系**
- 映射规则：
  - 自动匹配：Sheet 名称完全相同 → 自动配对
  - 手动匹配：对于名称不同的 Sheet，用户手动指定映射关系
  - 未匹配的 Sheet：跳过不处理

##### B. 智能裁剪表（核心创新点）

用户指定一个**时间/版本列**作为裁剪依据列（如"月份"列、"日期"列、"批次"列等）：

**裁剪逻辑：**
1. 读取结果表该列的所有唯一值，按时间/版本排序，得到集合 `R = {202601, 202602, 202603}`
2. 读取目标 Sheet 该列的所有唯一值，得到集合 `T = {202601, 202602}`
3. 求交集 `I = R ∩ T = {202601, 202602}`
4. **裁剪操作**：删除目标 Sheet 中该列值在交集 `I` 中的所有行
5. **填入操作**：将结果表中该列值在 `R`（即全部结果数据）中的所有行，追加到目标 Sheet 末尾（或按指定位置插入）

**效果示例：**

| 步骤 | 目标 Sheet | 结果表 |
|------|-----------|--------|
| 初始 | 月份: 202601, 202602（共 50 行数据） | 月份: 202601, 202602, 202603（共 75 行数据） |
| 裁剪后 | 删除 202601, 202602 的行 → 空表 | - |
| 填入后 | 202601, 202602, 202603 全部数据 → 共 75 行 | - |

**支持的高级选项：**
- `裁剪模式`:
  - **交集裁剪**（默认）: 删除目标表中与结果表有交集的月份数据，再填入结果表全部数据
  - **全量替换**: 直接清空目标 Sheet，填入结果表全部数据
  - **仅追加**: 不删除，仅将结果表中目标表不存在的月份数据追加到末尾
- `裁剪列可配置`: 用户可以指定任意一列作为裁剪依据（不限于时间，可以是任意列名）
- `裁剪预览`: 在正式执行前，展示"将被删除的 X 行"、"将被填入的 Y 行"，用户确认后执行

##### C. 列名对齐

- 结果表与目标 Sheet 的列名按名称精确匹配（与正向合并逻辑一致）
- 结果表中目标 Sheet 不存在的列 → 丢弃并记录日志
- 目标 Sheet 中结果表不存在的列 → 保留原值（或留空，可配置）
- "表名"列的处理：可选择填入来源标识或保留原值

##### D. 输出与安全

- **不修改原文件**：生成新文件 `{原文件名}_已刷新.xlsx`
- **预览确认**：执行前展示变更摘要（裁剪行数、填入行数、涉及的 Sheet）
- **操作日志**：记录每一步删除/填入的行数，写入 `反向填表日志.txt`
- **异常处理**：与正向合并一致，弹窗让用户决策

##### E. 界面交互设想

```
 ┌──────────────────────────────────────────────┐
 │  反向填表工具                                  │
 ├──────────────────────────────────────────────┤
 │  Step 1: 选择合并结果文件                       │
 │  [                     ] [浏览...]             │
 │                                                │
 │  Step 2: 选择待填入的目标文件                    │
 │  [                     ] [浏览...]             │
 │                                                │
 │  Step 3: Sheet 映射                            │
 │  ┌─────────────┬─────────────┬──────┐         │
 │  │ 结果表 Sheet │ 目标 Sheet  │ 状态  │         │
 │  ├─────────────┼─────────────┼──────┤         │
 │  │ NAME1       │ Sheet1      │ ✓自动│         │
 │  │ NAME2       │ [选择▼]     │ 手动 │         │
 │  │ NAME3       │ (无匹配)     │ 跳过 │         │
 │  └─────────────┴─────────────┴──────┘         │
 │                                                │
 │  Step 4: 裁剪设置                              │
 │  裁剪列: [月份        ▼]                       │
 │  裁剪模式: ○ 交集裁剪  ○ 全量替换  ○ 仅追加    │
 │  [预览变更]                                    │
 │                                                │
 │  ┌─ 预览 ─────────────────────────┐           │
 │  │ Sheet NAME1 → Sheet1:           │           │
 │  │   将删除 45 行 (202601,202602)  │           │
 │  │   将填入 75 行 (全部结果数据)    │           │
 │  │ Sheet NAME2 → Sheet2:           │           │
 │  │   将删除 30 行 (202601)         │           │
 │  │   将填入 50 行 (全部结果数据)    │           │
 │  └────────────────────────────────┘           │
 │                                                │
 │  [执行]  [取消]                                │
 └──────────────────────────────────────────────┘
```

### 5.4 技术要点

| 要点 | 说明 |
|------|------|
| 裁剪列排序 | 需要智能识别时间/版本格式（`202601`、`2026-01`、日期等），按语义排序而非字典序 |
| 交集计算 | 高效集合运算，支持大数据量 |
| 安全写入 | 遵循现有"内存缓存→一次性写入→不修改原文件"的安全策略 |
| 列映射复用 | 尽量复用正向合并的列名匹配逻辑（`process_small_table()` 中的列映射部分） |
| xlsx 读写 | 复用现有 `read_xlsx()` / `write_xlsx()` 零依赖方案 |

### 5.5 实现优先级建议

| 优先级 | 功能点 | 说明 |
|--------|--------|------|
| P0 | 基本反向填表（全量替换模式） | MVP：结果表数据完整覆盖目标 Sheet |
| P0 | 列名对齐匹配 | 与正向合并的匹配逻辑一致 |
| P1 | 智能裁剪（交集裁剪模式） | 核心创新功能，按时间列对齐裁剪 |
| P1 | 裁剪预览确认 | 用户可见变更范围后再执行 |
| P2 | 自动 Sheet 名称匹配 | 减少手动映射操作 |
| P2 | 仅追加模式 | 补充裁剪模式 |
| P3 | GUI 图形界面 | 与正向合并的 tkinter 风格一致 |

---

## 六、项目约定

### 6.1 代码风格
- Python 3.8+，零第三方依赖
- 中文注释 + 中文日志（面向中文用户）
- 变量命名：snake_case
- 内部函数以 `_` 开头

### 6.2 打包命令
```bash
pip install pyinstaller
pyinstaller --onefile --console 合并脚本.py
```
> 使用 `--console` 保留控制台窗口以便查看进度和日志。

### 6.3 Git 工作流
- 单分支 `main`，直接在 main 上开发
- Commit 前缀：`feat:` / `fix:` / `refactor:` / `docs:`
- 推送目标：`git@github.com:OrangeMoon-hub/Excel_combiner.git`
- 当前 HEAD：`d0d9ae7`

### 6.4 tkinter 红线 ⚠️
- **任何 widget 的 `padx`/`pady` 必须使用单个整数值，禁止使用元组**
- **`.pack()` 中的 `padx`/`pady` 同样是单整数**
- Windows tkinter 不支持元组间距，会报 `TclError: bad screen distance`

---

## 七、参考资料

| 资源 | 路径/链接 |
|------|----------|
| PRD 文档 | `PRD_Excel小表并大表工具.md` (v1.1, 2026-06-07) |
| 测试方案 | `测试文档/测试方案.md` (19 项用例) |
| GitHub | https://github.com/OrangeMoon-hub/Excel_combiner |
| 正向合并源码 | `合并脚本.py` (1340 行) |
| 测试数据 | `测试环境/` (被填充表.xlsx + 3 个小表) |
| excel-editor 脚手架 | `/home/ubuntu/excel-editor/` (空项目，规划中) |

---

> 📝 **本文档用途**: 作为 AI 助手的项目上下文，每次会话开始时读取，确保多轮开发一致。  
> 🔄 **更新频率**: 每次重大功能完成后更新。  
> 🦞 **最后更新**: 2026-06-21 by 皮皮
