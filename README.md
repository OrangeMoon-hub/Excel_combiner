# Excel 小表并大表 — 智能合并 & 拆分工具 V1.65

[![AI Vibe-Coding](https://img.shields.io/badge/AI_Vibe--Coding-🤖-purple)](https://github.com/OrangeMoon-hub/Excel_combiner)
[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Data integrity tests](https://github.com/OrangeMoon-hub/Excel_combiner/actions/workflows/tests.yml/badge.svg)](https://github.com/OrangeMoon-hub/Excel_combiner/actions/workflows/tests.yml)

## 1.65 数据正确性修复

- 修复多个 Sheet 映射到同一目标时，因列序不同导致姓名、金额等数据错列。
- 修复覆盖更新后多余的旧数据没有清空。
- 修复拆分值清理为相同文件名时混合分组或覆盖文件的问题，自动分配不重名的文件名并记录对应关系。
- 修复同名列选择“忽略(留空)”后仍填入第一列的值。
- 新增 29 项文件级自动化回归测试及 Windows/Linux CI。历史手工验收记录与本次自动化测试分开保留。

详见 [更新记录](CHANGELOG.md)。[标签 `1.65`](https://github.com/OrangeMoon-hub/Excel_combiner/tree/1.65) 用于浏览和下载该版本；让主分支恢复其内容时应创建新的恢复提交，以保留历史。

> **批量合并或拆分 Excel，减少重复复制粘贴，并把程序发现的数据异常记录下来。**
>
> 工具提供图形化操作，并以模板工作簿为基础写入数据。1.65 已自动验证四类数据正确性问题；复杂图表、绘图形状、VBA 运行效果和完整 GUI 流程不在当前自动验证范围内。
>
> 🤖 这是一个 AI 辅助开发项目：需求来自真实的数据工作场景。当前可信范围以本 README 的“验证记录”和“约束与边界”为准。

---

## ✨ 为什么用这个？

想象一下：月底了，几十个仓库/部门各发来一张 Excel，你要把它们拼成一张总表。

手动干：一个个打开、一行行复制粘贴、列名对不上还要自己调、对错全靠肉眼比……**一下午就这么没了，还可能出错。**

用这个工具：**选择模板和待处理文件后批量执行，并生成合并结果、异常记录和运行日志。** 实际耗时取决于文件数量、数据量、公式和工作簿复杂度，目前没有正式性能基准。

| 痛点 | 解决 |
|------|------|
| 列名不一致 | 自动匹配 + 检测异常（丢失列/多余列/同名列冲突） |
| 格式难统一 | 以模板表为基础写入；已验证有限的样式、公式和合并单元格场景，复杂对象见下方边界 |
| 出错了不知道 | 对程序已检测的异常生成记录表和日志 |
| 不会写代码 | 提供 tkinter 图形化对话框；可直接运行 Python 脚本，也可自行打包为 exe |
| 装环境麻烦 | 拆分工具使用标准库；合并工具通过 requirements.txt 安装 openpyxl |
| 大表需要拆分 | 内置拆分脚本，按列值一键拆分多 Sheet 大表 |

---

## 🚀 当前能力与验证边界

下面列出当前程序已经实现的行为。带有兼容性边界的能力，应结合自己的模板样例验收后再用于正式数据。

1. **🎨 基于模板写入** —— 合并结果从模板副本写出，而不是重新创建一个空工作簿。自动化测试已覆盖有限的单元格样式、公式和合并单元格保留场景。openpyxl 无法保证保存 Excel 中的所有绘图对象，复杂图表、图片、形状、外部链接等需要用实际模板单独验收。
2. **🔐 `.xlsm` 容器处理** —— `.xlsm` 模板会以 `keep_vba=True` 打开，并继续输出为 `.xlsm`。当前测试没有执行 VBA，也没有覆盖按钮、ActiveX、签名或外部连接，因此不能承诺宏及相关对象保存后仍可正常运行。
3. **🧠 同名列"三步判断"** —— 小表里出现两个"备注"列？它按「值一致→全填 / 数量相等→顺序 / 多于→弹窗问你」来智能决策，而不是报错罢工，或默默选错列埋雷。
4. **🔗 拆分→合并流程衔接** —— 默认情况下，拆分产出的文件名来自拆分值，Sheet 名保留原名，可继续作为合并输入。单 Sheet 可选择以拆分值重命名 Sheet，再由合并端确认目标映射。
5. **👀 写入前"匹配总览"预览 + 实时列名匹配度** —— 不像别的工具"点一下直接出结果，错没对只能事后翻"，它在写文件前先给你一张清楚的映射总览（✅/⚠️），手动指认时还能实时看到"匹配了几列"，心里有底再动手。
6. **🧾 处理记录** —— 程序检测到的丢弃列、额外列、取消和跳过等行为会写入异常记录表或日志。当前没有覆盖所有失败方式的完整审计保证。

---

## 🔧 两大工具

### 📥 合并脚本（合并脚本.py）

将多个结构相似的 Excel/CSV 小表按列名智能匹配合并到统一的模板表中。

```
运行 合并脚本.py（或自行打包的 exe）
      │
      ▼
  选择模板文件 + 可选"标注数据来源"
      │
      ▼
  选择待合并文件（多选，模板可参与合并）
      │
      ▼
  逐文件处理：遇到异常弹窗询问 → 用户决策
      │
      ▼
  生成 合并结果.xlsx（含异常记录 & 审计日志）
```

**V1.4 新增：**
- 拆分 → 合并的 Sheet 名衔接策略

**V1.5 新增：**
- openpyxl 模板写入：保留工作表结构，并在有限样例中验证样式、公式和合并单元格；复杂 Excel 对象需单独验收
- 匹配总览确认：合并前展示匹配结果，确认后再执行
- 合并结果覆盖更新（修复在职/试用期数据被追加到空行区之后）
- 修复工号 / OA 上级工号等文本数字列前导零丢失
- 默认合并读取路径和拆分解析器兼容 inlineStr 内联字符串；合并端旧的手写后备解析器仍待统一
- **处理 `.xlsm` 工作簿**：可作为模板/小表，使用 `keep_vba` 保留 VBA 部件并让输出扩展名跟随模板；宏运行和复杂控件尚未自动验证

**V1.3 新增：**
- "标注数据来源"可选（checkbox，默认勾上，向前兼容）
- 模板文件可被合并（列表标记 📌，默认不勾）
- 弹窗定位统一，不再跳来跳去
- 输出文件自动递增防覆盖（`合并结果_2.xlsx`...）

### 📤 拆分脚本（拆分脚本.py）

将一张含多 Sheet 的大表，按每个 Sheet 各自选定的列值拆分为多个小表文件。

```
运行 拆分脚本.py（或自行打包的 exe）
      │
      ▼
  选择大表文件（单选）
      │
      ▼
  配置每个 Sheet：勾选参与 + 选择拆分依据列
      │
      ▼
  生成 拆分输出/ 文件夹
      ├── 值A.xlsx  （含所有选中 Sheet，Sheet 名保留原名）
      ├── 值B.xlsx
      └── 值C.xlsx
```

**特性：**
- 每个 Sheet 独立选择拆分依据列
- 不同 Sheet 可选用不同列拆分
- 拆分列值取并集，同值自动汇入同一文件
- 文件名默认来自拆分列值，Sheet 名默认保留原始名称，可继续交给合并脚本处理
- 输出文件夹自动递增防覆盖（`拆分输出_2/`...）
- 非法文件名字符自动替换，空值行自动跳过

---

## 🖥️ 典型工作流

```
数据库导出的多 Sheet 大表
         │
         ▼
   拆分脚本.py  ← 按"数据类型"列拆分
         │
   拆分输出/
   ├── 入职.xlsx
   ├── 离职.xlsx
   └── 在职.xlsx
         │
         ▼
   合并脚本.py  ← 选模板 + 不勾"标注来源"
         │
         ▼
   合并结果.xlsx（可再作为下次合并的数据源）
```

---

## 📋 合并脚本功能一览

### 智能文件管理
- 自动扫描目录内所有 `.xlsx` / `.xlsm` / `.csv` 文件
- 图形化单选模板 + 多选待合并文件（勾选变色 + 全选按钮）
- 排除 `~$` 临时锁文件

### 列名匹配 & 冲突处理

| 场景 | 行为 |
|------|------|
| 列名完全一致 | 自动匹配，按列填值 |
| **同名列**（小表多列同名） | 三步判断：值一致→全填 / 数量相等→顺序 / 多于→逐列弹窗 |
| 列名不存在于模板 | 弹窗确认后丢弃，记录异常 |
| 数据列数超过表头 | 弹窗确认后截断，逐额外列记录 |
| 表头有空列名 | 弹窗确认后丢弃，标注"数据异常，无列名" |

### 结果输出

| Sheet | 内容 |
|-------|------|
| 模板 Sheet(s) | 模板表头 + 所有合并数据行 |
| `异常记录` | 程序检测到的四类异常记录（含样例值、列位置、用户操作） |
| `取消合并的表名` | 被取消合并的文件及原因 |

### 异常记录样例

```
文件名     Sheet    行号    列名             列1位置  列1值        异常类型
小表3.xlsx  NAME1   全部行   数据异常，无列名    C       v1,v2,v3    无列名
小表3.xlsx  NAME1   全部行   -                 I       45,67,AA;    超表头列数
小表2.xlsx  NAME2   -       111               L       111,222       列名不存在于大表
小表1.xlsx  NAME1   -       T                 K       落选值1,2     同名列未选择
```

---

## 📋 拆分脚本功能一览

| 功能 | 说明 |
|------|------|
| 多 Sheet 独立配置 | 每个 Sheet 可各自勾选是否参与、各自选拆分列 |
| 拆分列默认第一列 | 下拉框默认选中该 Sheet 第一列 |
| 同值汇集 | 所有选中 Sheet 的拆分列值取并集，一个唯一值一个文件 |
| 无匹配行保留表头 | 某输出文件中某 Sheet 无匹配行时保留表头，结构不丢 |
| 安全处理 | 非法文件名自动替换 `_`，空值行跳过并记录日志 |
| 输出防覆盖 | 自动递增 `拆分输出_2/` `拆分输出_3/`... |

---

## ✅ 验证记录

这里区分“当前可重复的自动化证据”和“历史人工记录”。绿色 CI 只代表列出的自动化检查通过，不代表所有 Excel 功能都已验证。

### 1.65 自动化证据

| 检查 | 验证内容 | 当前结果 |
|------|---------|------|
| **29 项文件级回归测试** | 测试创建临时 Excel/CSV，调用生产处理函数，重新打开输出并核对数据 | ✅ CI 通过 |
| **四类数据错误** | 多 Sheet 错列、旧行残留、拆分名称冲突、同名列忽略失效 | ✅ 有回归覆盖 |
| **附加边界** | CSV BOM/空行/前导零、文件名冲突、有限的样式/公式/合并单元格场景 | ✅ 有回归覆盖 |
| **环境矩阵** | Ubuntu 22.04 / Windows 2022 × Python 3.8 / 3.13 | ✅ 四种组合通过 |

测试中的弹窗选择由 mock 模拟；测试调用真实读入、处理和写出函数，但没有从 GUI 或打包后的 exe 完成整条用户操作链。因此这里称为“文件级回归测试”，不称为完整 E2E。

### 历史人工记录

仓库中的 `测试文档/测试方案.md` 记录了早期 19 项结构化用例，README 旧版本还记录过 5 轮人工检查。这些记录可用于了解开发历史，但没有在 1.65 中自动重跑，也不能替代当前 CI。完整 GUI、打包 exe、VBA 运行、复杂图表/绘图对象、性能和大规模文件兼容性仍待专项验证。

---

## 🛠️ 技术栈

| 维度 | 选择 |
|------|------|
| 语言 | Python 3.8+ |
| GUI | tkinter（标准库） |
| Excel 解析 | 合并优先使用 openpyxl；拆分使用 zipfile + xml.etree |
| Excel 写入 | 合并使用 openpyxl 写入模板；拆分使用标准库 |
| CSV 读取 | csv（标准库） |
| 打包 | PyInstaller `--onefile` |
| 依赖 | **合并和自动化测试需要 openpyxl**；拆分无需第三方依赖 |

---

## 📦 项目结构

```
Excel_combiner/
├── 合并脚本.py                          # 正向合并工具（需要 openpyxl）
├── 拆分脚本.py                          # 大表拆分工具（标准库）
├── PRD_Excel小表并大表工具.md            # 产品需求文档
├── AI_CONTEXT.md                       # AI 项目记忆（坑位记录 + 反向填表设计）
├── requirements.txt                    # 合并工具和测试的依赖
├── CHANGELOG.md                        # 版本修复记录与标签使用说明
├── tests/test_regressions.py            # 文件级自动化回归测试
├── .github/workflows/tests.yml          # Windows/Linux 自动运行测试
├── 测试文档/
│   └── 测试方案.md                      # 19 项结构化测试用例
├── 测试环境/                            # 可运行样例数据（下载后即可跑通）
│   ├── 小表.xlsx / 小表2.xlsx / 小表3.xlsx  # 合并测试数据
│   ├── 被填充表.xlsx                    # 合并模板样例
│   └── 反向填表/
│       ├── 大表导出_1月.xlsx / 大表导出_2月.xlsx  # 拆分测试数据
│       ├── 模板.xlsx / 特殊字符大表.xlsx
│       └── 模拟业务数据/                  # 拆分→合并闭环模拟数据
├── .gitignore
├── LICENSE                             # MIT 开源协议
└── README.md
```

---

## 🚀 本地运行 / 打包

### 直接运行

```bash
python -m pip install -r requirements.txt
python 合并脚本.py
python 拆分脚本.py
```

> 合并工具的模板写入需要 openpyxl；缺少依赖时不能完成合并输出。拆分工具仍可仅用标准库运行。

### 自动化测试

```bash
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
```

测试会在临时目录创建 Excel/CSV 样例，调用实际处理函数，再打开输出核对值。弹窗选择由测试模拟，不需要弹出窗口或启动 Excel。覆盖多 Sheet 映射、覆盖清空、拆分分组与文件名冲突、同名列选择，以及 CSV BOM、空行和前导零。

每次推送分支或提交 PR 时，GitHub Actions 自动运行 Windows/Linux × Python 3.8/3.13 四种组合。当前 `main` 尚未启用分支保护，因此 CI 会报告结果，但还没有强制阻止未通过检查的代码进入主分支。完整 GUI 操作、打包 exe、VBA 与复杂图表仍需单独验收。

### 打包为 exe

```bash
pip install pyinstaller openpyxl
pyinstaller --onefile --console 合并脚本.py
pyinstaller --onefile --console 拆分脚本.py
```

> 使用 `--console` 保留控制台窗口，方便查看进度和日志。
> 打包合并工具时必须包含 openpyxl。上述命令是本地打包方法；当前 CI 没有构建或测试 exe，仓库也没有把 exe 作为 1.65 的自动发布产物。

---

## ⚠️ 约束 & 边界

- **合并脚本**：模板文件第 1 列如勾选"标注数据来源"则必须为 `表名`
- Excel 数据处理面向 `.xlsx` / `.xlsm`（不支持旧式 `.xls`）；`.xlsm` 使用 `keep_vba` 保存 VBA 部件，但宏运行、按钮、ActiveX、签名和外部连接尚未验证
- openpyxl 不保证保留工作簿中的全部绘图对象；复杂图表、图片和形状应先用副本验收。[openpyxl 官方文档](https://openpyxl.readthedocs.io/en/stable/tutorial.html#loading-from-a-file)明确提示既有文件中的形状可能在打开并保存后丢失
- 当前 `requirements.txt` 只安装 openpyxl，没有安装 Pillow；不要把图片保留视为 1.65 的已验证能力
- 1.65 自动化重点验证数据值及有限的样式、公式、合并单元格场景，不构成完整格式保真保证
- CSV 以 UTF-8 读取，映射到虚拟 Sheet `Sheet1`，仅当模板含同名 Sheet 时匹配
- Windows tkinter 不支持 `pady=tuple` 写法（`pady=(15,5)` 报 `bad screen distance`），间距必须用单整数
- **拆分脚本**：Sheet 名最长 31 字符（Excel 限制），非法文件名字符自动替换

完整边界情况见 [PRD](PRD_Excel小表并大表工具.md)。

---

## 🔭 未来方向

候选功能之一是 **反向填表（智能裁剪替换）**：把“多张小表→合并大结果表”反向，用结果表按时间或版本刷新多 Sheet 目标文件。

目前只有“交集裁剪 / 全量替换 / 仅追加”等设计草案，代码尚未实现，也没有承诺发布版本或日期。设计背景见 `AI_CONTEXT.md` 第五章；该文件包含历史上下文，实际行为仍以当前代码、测试和本 README 为准。

---

## 🚧 未完成事宜 & 开发路线图

路线图表示当前优先级判断，不构成发布时间或功能保证。数据正确性和可验证性优先于新增功能。

### P0 — 发布可信度与兼容边界

- 合并或移除两份重复的手写 xlsx 解析逻辑，补齐合并端后备解析器的 inlineStr 测试。
- 建立图表、图片、形状、公式、宏和外部链接的样例矩阵，只承诺实际验证过的类型。
- 更新历史手工测试文档，使其与 1.65 的自动化测试范围一致。
- 为 `main` 启用分支保护，把数据完整性 CI 设为合并条件。

### P1 — 核心层与 CLI

- 将读取、匹配、合并、拆分和写入从 tkinter 弹窗及全局状态中分离。
- GUI 只负责收集选择和展示结果，核心层通过参数和返回值工作。
- 新增 CLI 入口及 subprocess 黑盒测试，检查退出码、日志和最终工作簿。
- 在 Windows CI 构建并冒烟测试 exe 后，再把 exe 作为正式发布产物。

### P2 — 候选业务功能

- 在完成行为契约、输入/期望输出样例和失败边界后，再实现反向填表。
- 根据真实用户日志和反馈决定性能优化、配置文件及其他功能的顺序。

### 已知技术债

| 项 | 说明 | 优先级 |
|----|------|--------|
| 单文件与界面耦合 | 两个生产脚本合计约 2681 行，核心处理仍会调用弹窗并依赖全局状态 | P1 |
| 解析器重复 | 合并和拆分各维护一份标准库 xlsx 解析器，修复容易只进入一份 | P1 |
| 入口测试缺失 | 目前没有 CLI；GUI、打包 exe 和完整用户流程未自动测试 | P1 |
| 重复读取 | 合并总览和正式处理阶段会再次读取小表，增加耗时和状态不一致风险 | P2 |
| 自动化范围 | 1.65 已有 29 项数据回归；宏、复杂格式、性能和大规模数据仍待补测 | P2 |
| Excel 命名空间兼容不确定 | `ns0` 前缀已修复，但未验证覆盖所有 Excel 版本 | P2 |
| 无配置文件 | 行为全硬编码，无用户自定义选项 | P3 |

---

## 👤 作者

**OrangeMoon** (橙月君)

- GitHub: [@OrangeMoon-hub](https://github.com/OrangeMoon-hub)

---

## 📄 License

MIT © OrangeMoon

---

## 📮 联系我

> **想要新功能？发现 bug？欢迎邮件联系：**
>
> 📧 **836972052@qq.com**
>
> 任何想法、建议、问题都可以发到这个邮箱，会尽快回复。
