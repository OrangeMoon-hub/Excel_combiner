# 更新记录

## 1.65 — 2026-09-19

本版本修复四类会产生错误结果的数据处理问题，标签名为 `1.65`。

- **多个 Sheet 汇入同一目标**：每个来源 Sheet 先按自己的表头匹配列，再按来源 Sheet 顺序追加。列序或列数不同不再造成错列。异常记录使用来源 Sheet 名，取消任一来源 Sheet 时仍取消整个文件。
- **覆盖后的旧数据**：正确清空多余尾部行的值，保留单元格样式和合并区域。显式传入空值时也会清除旧值。未参与写入的 Sheet 保持原样。
- **拆分名称冲突**：按未经文件名清理的分组值汇集数据（延续去除首尾空白的规则），输出时才分配文件名。非法字符替换、31 字符截断、大小写或已有后缀导致重名时自动追加 `_2`、`_3` 等；不同分组不会混合或互相覆盖。Windows 保留设备名自动加前缀。日志记录分组值与文件名的对应关系。
- **同名列忽略选择**：点击“忽略(留空)”后保持空值，不再重新填入第一列。
- **自动化回归**：新增 29 项 `unittest` 测试，使用临时 Excel/CSV 文件验证读入、处理、写出和重新打开后的结果。GitHub Actions 配置 Windows/Linux、Python 3.8/3.13 四种组合。
- **运行依赖**：增加 `requirements.txt`，明确合并写入需要 openpyxl，纠正 README 中自动降级写入的说明。

本次自动化测试模拟弹窗选择，不替代完整图形界面验收，也不代表 VBA、图表和所有 Excel 格式特性均已验证。反向填表仍属后续开发范围。

### 本地运行测试

```bash
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
```

### 标签与恢复版本

标签保存的是某次提交的位置。GitHub 仓库的 Tags 页面可以浏览或下载该版本。

查看 `1.65` 的本地代码（请先处理未提交修改）：

```bash
git fetch origin --tags
git switch --detach 1.65
```

返回主分支：

```bash
git switch main
```

切换标签只改变本地检出的版本，不会让 GitHub 的 `main` 自动回退。若要恢复远程主分支的内容，优先创建新的恢复提交；这样可以保留之后的提交历史。
